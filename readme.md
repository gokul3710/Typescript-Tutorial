<!-- 
        npm typescript -g (for global installation of typescript <br> 

        tsc tsfilename.ts destinationfilename.js (for executing typescript file) <br>

        tsc --init to generate tsconfig<br>
            set rootdir and outdir<br>
            set include only from rootsdir<br>

        tsc -w for automatically compilation 

        no emit on error in tsconfig - wont create a js file if there is error (tsc --noEmitOnError -w)

-->





